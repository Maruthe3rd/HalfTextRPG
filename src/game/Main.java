package game;

public class Main {
    public static void main(String[] args) {
        // Dependencies
        //System.out.println("Hello World");
        //SceneManager sceneManager = new SceneManager();
        //DatabaseManager dbManager = new DatabaseManager();
        //GameContext context = new GameContext(sceneManager, dbManager);

        // Plug-and-Play Modular Components
        //sceneManager.registerScene(new MainMenuScene());
        //sceneManager.registerScene(new TownScene());
        //sceneManager.registerScene(new CombatScene());

        // Boot-up point
        //sceneManager.setActiveScene("MAIN_MENU");
        //sceneManager.run(context);
    }
}